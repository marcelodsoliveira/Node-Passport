import passport from "passport";

// passport.use( ... ); // This is where you would configure your authentication strategy

export default passport;
// This file is used to configure the passport authentication strategy.